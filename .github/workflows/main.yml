name: Build and Deploy

on:
  push:
    branches:
      - main
env: 
  CI: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build React app
        run: npm run build -- --no-warnings  # Passing --no-warnings to ignore warnings as errors

      - name: Archive production build
        uses: actions/upload-artifact@v2
        with:
          name: my-app-artifact
          path: build  # Assuming 'build' is the directory containing your production build

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
        with:
          ACCESS_TOKEN: ${{ secrets.ACTIONS_DEPLOY_ACCESS_TOKEN }}
          BRANCH: gh-pages
          FOLDER: build
permissions:
  id-token: write
